version: 2

before:
  hooks:
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X github.com/CosmoLabs-org/SmokeSig/cmd.Version={{.Version}}
    binary: smokesig

archives:
  - format: tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- tolower .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "arm64" }}aarch64
      {{- else }}{{ .Arch }}{{ end }}
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^chore:"

brews:
  - repository:
      owner: CosmoLabs-org
      name: homebrew-tap
    directory: Formula
    homepage: "https://github.com/CosmoLabs-org/SmokeSig"
    description: "Universal smoke test runner — does it turn on?"
    test: |
      system "#{bin}/smokesig version"
    install: |
      bin.install "smokesig"

dockers:
  - dockerfile: Dockerfile
    extra_files:
      - go.mod
      - go.sum
    image_templates:
      - "cosmolabs/smokesig:{{ .Version }}"
      - "cosmolabs/smokesig:latest"
    goos: linux
    goarch: amd64
  - dockerfile: Dockerfile
    image_templates:
      - "cosmolabs/smokesig:{{ .Version }}-arm64"
      - "cosmolabs/smokesig:latest-arm64"
    goos: linux
    goarch: arm64

release:
  github:
    owner: CosmoLabs-org
    name: SmokeSig
  prerelease: auto
